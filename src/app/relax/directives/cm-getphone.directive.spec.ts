import { CmGetphoneDirective } from './cm-getphone.directive';

describe('CmGetphoneDirective', () => {
  it('should create an instance', () => {
    const directive = new CmGetphoneDirective();
    expect(directive).toBeTruthy();
  });
});
